#include<stdio.h>

int main(void)
{
    int totalnum;
    
    scanf("%d", &totalnum);

    for(int i=0;i<totalnum;i++)
    {
        int a1,a2,temp;
        scanf("%d %d", &a1, &a2);
        temp=a1;
        a1=a2;
        a2=temp;
        printf("%d %d\n", a1, a2);
    }

    return 0;
}
